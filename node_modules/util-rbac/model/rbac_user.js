module.exports = function(mongoose) {
	var RBAC_UserSchema = mongoose.Schema({
		user: {
			type: String,
			unique: true
		},
		passwd: String,
		roles: [{
			type: mongoose.Schema.Types.ObjectId,
			ref: 'RBAC_role'
		}]
	});

	return mongoose.model('RBAC_user', RBAC_UserSchema, 'rbac_user');
};